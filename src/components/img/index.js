const logos = {
    blender: require('./blender.png').default,
    csharp: require('./c-sharp.png').default,
    unity: require('./unity.png').default,
    photoshop: require('./photoshop.png').default
}

export default logos;